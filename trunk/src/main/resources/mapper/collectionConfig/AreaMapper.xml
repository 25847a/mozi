<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fadl.collectionConfig.dao.AreaMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.fadl.collectionConfig.entity.Area">
        <id column="id" property="id" />
        <result column="isDelete" property="isDelete" />
        <result column="province" property="province" />
        <result column="county" property="county" />
        <result column="town" property="town" />
        <result column="roadFee" property="roadFee" />
        <result column="createDate" property="createDate" />
        <result column="updateDate" property="updateDate" />
        <result column="creater" property="creater" />
        <result column="updater" property="updater" />
        <result column="plasmaId" property="plasmaId" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, isDelete, province, county, town, createDate, updateDate, creater, updater, plasmaId
    </sql>

</mapper>